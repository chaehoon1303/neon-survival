/*
 * Public browser configuration only.  Keep all provider secrets and service
 * role keys on the account API server; this file is intentionally blank until
 * the hosted Supabase + Worker services are created.
 */
window.NEON_ACCOUNT_CONFIG = {
  supabaseUrl: '',
  supabasePublishableKey: '',
  progressApiBase: ''
};
